version: '3'


services:
  nginx:
    build: .
    restart: always
    container_name: gateway_nginx
    ports:
      - 1337:80
    networks:
      - gateway_network
    command: ["sh", "-c", "sleep 5 && nginx -g 'daemon off;'"]


networks:
  gateway_network:
    external: true
